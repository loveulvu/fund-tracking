# Current Go backend runtime
MONGO_URI=mongodb://localhost:27017/fund_tracking
JWT_SECRET=replace_with_a_strong_secret
UPDATE_API_KEY=
APP_VERSION=dev
APP_BUILT_AT=

# Optional local/server port override
PORT=8081

# Optional behavior/config for legacy scripts
AUTO_REFRESH_INTERVAL_SECONDS=180

# Legacy Flask/email monitor only; not used by the current Go auth flow
EMAIL_SENDER=
EMAIL_PASSWORD=

# Optional build metadata
GIT_COMMIT=
SOURCE_VERSION=

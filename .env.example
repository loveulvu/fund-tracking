# Backend runtime
MONGO_URI=mongodb://localhost:27017/fund_tracking
JWT_SECRET=replace_with_a_strong_secret
PORT=8080

# Optional behavior/config
AUTO_REFRESH_INTERVAL_SECONDS=180
UPDATE_API_KEY=
APP_VERSION=dev
APP_BUILT_AT=

# Optional email verification config (required for register/resend email send)
EMAIL_SENDER=
EMAIL_PASSWORD=

# Optional build metadata
GIT_COMMIT=
RAILWAY_GIT_COMMIT_SHA=
SOURCE_VERSION=

